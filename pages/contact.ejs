<!DOCTYPE html>
<html lang="fr" class="h-100">
<head>
    <!-- Définition du jeu de caractères et de la taille de la fenêtre -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Titre de la page -->
    <title><%= title %></title>
    <!-- Feuille de style de Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Feuille de style de Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* Définition du layout en colonne */
        body {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        /* Définition de la hauteur du contenu principal */
        main {
            flex: 1;
        }
    </style>
</head>
<body class="d-flex flex-column h-100 <%= locals.isAdmin ? 'bg-dark text-white' : '' %>">
    <!-- Inclusion du header -->
    <%- include('../components/header') %>

    <!-- Contenu principal -->
    <main class="flex-shrink-0">
        <div class="container mt-4">
            <h1>Contactez-nous</h1>
            <!-- Contenu de contact ici -->
        </div>
    </main>

    <!-- Inclusion du footer -->
    <%- include('../components/footer') %>

    <!-- Fichier JavaScript de Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
