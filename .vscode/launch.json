{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "node",
        "request": "launch",
        "name": "Serveur HTTP",
        "program": "${workspaceFolder}/HTTP/server.js",
        "preLaunchTask": "nodemon"
      }
    ]
  }